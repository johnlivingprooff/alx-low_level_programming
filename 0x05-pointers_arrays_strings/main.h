#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>

/* All the Function prototypes for this project */
int _putchar(char c);
void reset_to_98(int *n);
void swap_int(int *a, int *b);
int _strlen(char *s);
void _puts(char *str);

#endif
